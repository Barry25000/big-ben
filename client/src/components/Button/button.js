
import { User } from '../../../../server/models';
import Users from '../models/Users';
// import Orders from '../models/Orders';
// TODO:  add test button here for login

// if the user password and email match, then log them in when they click the button
// if the user password and email do not match, then do not log them in when they click the button
// if the user password and email do not match, then display an error message when they click the button


const button = document.querySelector('button[name="button"]');